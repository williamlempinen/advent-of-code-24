use std::io;

fn read_input_to_matrice(path: &str) -> Result<Vec<Vec<&str>>, io::Error> {
    let mut matrice: Vec<Vec<&str>> = Vec::new();
    Ok(matrice)
}

fn main() {
    println!("Day 6!");

    let path = "./src/inputs/day06input";
}
